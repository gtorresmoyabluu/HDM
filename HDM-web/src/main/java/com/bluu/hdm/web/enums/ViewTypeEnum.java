package com.bluu.hdm.web.enums;

public enum ViewTypeEnum {

    add, detail, edit, list, search, upload, chart, synchronization, incorporate, access;

    public String getName() {
	return name();
    }
}
